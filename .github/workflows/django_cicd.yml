name: Django CICD

on:
  workflow_dispatch:
  push:
    # branches:
    # - master
    paths:
    - "alteredbuilder/**"
    - ".github/workflows/django_cicd.yml"
    - "!alteredbuilder/config/__init__.py"

env:
  DEBUG: False
  USE_GCS_STATICS: False
  IMAGE_NAME: ${{ vars.CLOUD_RUN_SERVICE_NAME }}


defaults:
  run:
    working-directory: ./alteredbuilder

jobs:
  # changes:
  #   name: Parse the changed files
  #   runs-on: ubuntu-22.04
  #   outputs:
  #     statics: ${{ steps.filter.outputs.statics }}
  #     migrations: ${{ steps.filter.outputs.migrations }}
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4
  #   - name: Filter changed files
  #     uses: dorny/paths-filter@v3
  #     id: filter
  #     with:
  #       filters: |
  #         statics:
  #         - "**/static/**"
  #         - "**/statics/**"
  #         - "alteredbuilder/requirements.txt"
  #         migrations:
  #         - "**/migrations/**"
  #         - "alteredbuilder/requirements.txt"

  build_image:
    name: Build the image
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - name: Build and export
      uses: docker/build-push-action@v6
      with:
        context: alteredbuilder
        tags: ${{ vars.AR_REPOSITORY }}/${{ vars.CLOUD_RUN_SERVICE_NAME }}
        outputs: type=docker,dest=/tmp/${{ env.IMAGE_NAME }}.tar
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.IMAGE_NAME }}
        path: /tmp/${{ env.IMAGE_NAME }}.tar

  # unittests:
  #   name: Run unittests
  #   needs: build_image
  #   runs-on: ubuntu-22.04
  #   services:
  #     db:
  #       image: postgres:15.4
  #       ports:
  #       - 5432:5432
  #       env:
  #         POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
  #         POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
  #         POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4
  #   - name: Download artifact
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: ${{ env.IMAGE_NAME }}
  #       path: /tmp
  #   - name: Load image
  #     run: |
  #       docker load --input /tmp/${{ env.IMAGE_NAME }}.tar
  #   - name: Run Tests
  #     uses: addnab/docker-run-action@v3
  #     with:
  #       image: ${{ vars.AR_REPOSITORY }}/${{ vars.CLOUD_RUN_SERVICE_NAME }}
  #       run: python manage.py test
  #       options: |
  #         -e SECRET_KEY=${{ secrets.SECRET_KEY }}
  #         -e DATABASE_URL=psql://${{ secrets.POSTGRES_USER }}:${{ secrets.POSTGRES_PASSWORD}}@db:5432/${{ secrets.POSTGRES_DB }}

  # push_image:
  #   name: Push the image
  #   needs: build_image
  #   runs-on: ubuntu-22.04
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4
  #   - name: Download artifact
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: ${{ env.IMAGE_NAME }}
  #       path: /tmp
  #   - name: Load image
  #     run: docker load --input /tmp/${{ env.IMAGE_NAME }}.tar
  #   - name: Authenticate into GCP
  #     id: auth
  #     uses: google-github-actions/auth@v2
  #     with:
  #       credentials_json: ${{ secrets.GCP_GITHUB_SA }}
  #   - name: Set up gcloud
  #     uses: google-github-actions/setup-gcloud@v2
  #   - name: Set up docker
  #     run: gcloud auth configure-docker ${{ vars.REGION }}-docker.pkg.dev --quiet
  #   - name: Push image
  #     run: docker push ${{ vars.AR_REPOSITORY }}/${{ vars.CLOUD_RUN_SERVICE_NAME }}

  migrate_db:
    name: Perform db migrations
    needs: [build_image]
    # if: needs.changes.outputs.migrations == 'true'
    runs-on: ubuntu-22.04
    # services:
    #   db:
    #     image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.12.0
    #     ports:
    #     - 5432:5432
    #     env:
    #       POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
    #       POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
    #       POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Download artifact
      uses: actions/download-artifact@v4
      with:
        name: ${{ env.IMAGE_NAME }}
        path: /tmp
    - name: Load image
      run: |
        docker load --input /tmp/${{ env.IMAGE_NAME }}.tar
    # - name: Configure Google Cloud SQL Auth Proxy
    #   uses: mattes/gce-cloudsql-proxy-action@v1
    #   with:
    #     creds: ${{ secrets.GCP_GITHUB_SA }}
    #     instance: ${{ vars.CLOUD_SQL_INSTANCE }}
    
    - name: Google auth
      id: auth
      uses: google-github-actions/auth@v1
      with:
        token_format: 'access_token'
        access_token_lifetime: '120s'
        create_credentials_file: true
        credentials_json:  '${{ secrets.GCP_GITHUB_SA_JSON }}'
        service_account:  '${{ secrets.GCP_GITHUB_SA_NAME }}'
    - name: Configure Google Cloud SQL Auth Proxy
      run: |
        docker run --net host --name db -d --expose 5432 gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.12.0 --address 0.0.0.0 -t ${{steps.auth.outputs.access_token}} --run-connection-test ${{ vars.CLOUD_SQL_INSTANCE }}
    - name: Run migrations
      run: |
        docker run --net host -e SECRET_KEY=${{ secrets.SECRET_KEY }} -e DATABASE_URL=${{ secrets.MIGRATION_DATABASE_URL }} ${{ vars.AR_REPOSITORY }}/${{ vars.CLOUD_RUN_SERVICE_NAME }} python manage.py migrate --no-input
    - name: sql logs
      if: ${{ always() }}
      run: docker logs db
    # - name: Run migrations
    #   uses: addnab/docker-run-action@v3
    #   with:
    #     image: ${{ vars.AR_REPOSITORY }}/${{ vars.CLOUD_RUN_SERVICE_NAME }}
    #     run: python manage.py migrate --no-input
    #     options: |
    #       -e SECRET_KEY=${{ secrets.SECRET_KEY }}
    #       -e DATABASE_URL=${{ secrets.MIGRATION_DATABASE_URL }}

  # collect_statics:
  #   name: Collect static files into GCS
  #   needs: [build_image]
  #   # if: needs.changes.outputs.statics == 'true'
  #   runs-on: ubuntu-22.04
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4
  #   - name: Download artifact
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: ${{ env.IMAGE_NAME }}
  #       path: /tmp
  #   - name: Load image
  #     run: |
  #       docker load --input /tmp/${{ env.IMAGE_NAME }}.tar
  #   - name: Collect static files to GCS
  #     uses: addnab/docker-run-action@v3
  #     with:
  #       image: ${{ vars.AR_REPOSITORY }}/${{ vars.CLOUD_RUN_SERVICE_NAME }}
  #       run: python manage.py collectstatic --no-input
  #       options: |
  #         -e SECRET_KEY=${{ secrets.SECRET_KEY }}
  #         -e DATABASE_URL=psql://${{ secrets.POSTGRES_USER }}:${{ secrets.POSTGRES_PASSWORD}}@db:5432/${{ secrets.POSTGRES_DB }}
  #         -e USE_GCS_STATICS=True
  #         -e GCS_BUCKET_STATICS=${{ vars.GCS_BUCKET_STATICS }}
  #         -e GCP_GITHUB_SA=${{ secrets.GCP_GITHUB_SA }}

  # deploy:
  #   name: Deploy to Cloud Run
  #   needs: [build_and_push_image, migrate_db, collect_statics]
  #   if: |
  #     always() &&
  #     !contains(needs.*.result, 'failure') &&
  #     !contains(needs.*.result, 'cancelled')
  #   runs-on: ubuntu-22.04
  #   permissions:
  #     contents: 'read'
  #     id-token: 'write'
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4
  #   - name: Authenticate into GCP
  #     id: auth
  #     uses: google-github-actions/auth@v2
  #     with:
  #       credentials_json: ${{ secrets.GCP_GITHUB_SA }}
  #   - name: Create SHORT_SHA variable
  #     id: vars
  #     run: echo "sha_short=$(git rev-parse --short ${{ github.sha }})" >> $GITHUB_OUTPUT
  #   - name: Deploy to Cloud Run
  #     uses: google-github-actions/deploy-cloudrun@v2
  #     with:
  #       project_id: ${{ vars.GCP_PROJECT }}
  #       region: ${{ vars.REGION }}
  #       service: ${{ vars.CLOUD_RUN_SERVICE_NAME }}
  #       image: ${{ vars.AR_REPOSITORY }}/${{ vars.CLOUD_RUN_SERVICE_NAME }}
  #       env_vars: |
  #         COMMIT_ID=${{ steps.vars.outputs.sha_short }}
  #       env_vars_update_strategy: merge
  #       flags: |
  #         --platform=managed
  #         --port=8000
  #         --add-cloudsql-instances=${{ vars.CLOUD_SQL_INSTANCE }}
  #         --allow-unauthenticated
  #         --service-account=${{ vars.CLOUD_RUN_SA_NAME }}
  #         --cpu=1000m
  #         --memory=256Mi